<% @posts.each do |post| %>
	<h2><%= post.title %></h2>
	<div><%= image_tag post.image.url(:thumb) %></div>
	<div><%= truncate(post.body, :length => 150) %></div>
	<br>
	<%= link_to "Read Post", post %>
<% end %>